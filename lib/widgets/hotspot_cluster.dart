import 'package:flutter/material.dart';

class HotspotCluster extends StatelessWidget {
  const HotspotCluster({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(); // Placeholder for hotspot cluster widget
  }
}
